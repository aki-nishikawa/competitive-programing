#include<bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
typedef long long ll;
int main(){
    double x, y, k;    cin >> x >> y >> k;
     cout << fixed << setprecision(15) << y - k/x << endl;
    return 0;
}
